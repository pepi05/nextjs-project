import { Typography } from "@material-ui/core";

const Footer = ({ classes }) => {
  return (
    <footer className={classes.footer}>
      <Typography>All rights reserved. Petar App</Typography>
    </footer>
  );
};

export default Footer;
